# run_coverage.R — shared coverage skill (STUB, no logic yet)
#
# Purpose (once implemented): report spatial/temporal coverage of a dataset
# against the minimal data contract (columns lat, lon, time, value) — e.g.
# which cells/months have data, sampling effort, and gaps.
#
# Reads the minimal data contract, NOT a named local file.

run_coverage <- function(data, ...) {
  # TODO: implement coverage computation. Scaffold only — no logic in this phase.
  stop("run_coverage() not implemented — scaffold stub.")
}
